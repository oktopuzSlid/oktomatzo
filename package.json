{
  "name": "oktomatzo-host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "dev:all": "concurrently \"astro dev\" \"cd backend && .venv\\Scripts\\uvicorn app.main:app --reload --port 8001\"",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
