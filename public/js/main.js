// ============================================================
// main.js — SITE-WIDE features (future: theme toggle, etc.)
// ============================================================
// Nav highlighting is handled by Astro's data-current attribute.
// main.js is for browser-only features that don't need build-time.
// ============================================================

document.addEventListener('DOMContentLoaded', function () {
  console.log('Oktomatzo Host loaded')
})
